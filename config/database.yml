default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: example
  host: db  # Matches the service name in docker-compose.yml
  port: 5432

development:
  <<: *default
  database: travel_matcher_development

test:
  <<: *default
  database: travel_matcher_test